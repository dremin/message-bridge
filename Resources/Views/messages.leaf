<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html lang="en">
    <head>
        <title>#(chat.name) - Messages</title>
    </head>
    <body>
        <h1><a href="/lite">Chats</a> / #(chat.name)</h1>
        <b>Send message:</b>
        <form action="/lite" method="post">
            <input type="text" name="message" size="40" autocomplete="off">
            <input type="submit" value="Send">
            <input type="hidden" name="address" value="#(chat.replyId)" />
            <input type="hidden" name="chatId" value="#(chat.id)" />
        </form>
        <br>
#for(message in messages):
        <hr>
        <b>
    #if(message.isMe == false):
        #(message.from)
    #else:
        Myself
    #endif
        </b> <em>(#(message.received))</em><br>
        <p>#(message.body)</p>
    #if(message.attachments):
        #for(attachment in message.attachments):
            #if(attachment.type == "image/jpeg" || attachment.type == "image/png" || attachment.type == "image/tiff" || attachment.type == "image/heic"):
                <a href="/attachments/#(attachment.id)/thumb?download=true&amp;maxSize=500">#(attachment.filename)</a><br>
            #else:
                <a href="/attachments/#(attachment.id)">#(attachment.filename)</a><br>
            #endif
        #endfor
    #endif
#endfor
    </body>
</html>
